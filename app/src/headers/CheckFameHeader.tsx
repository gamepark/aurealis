import { HeaderText } from '@gamepark/react-game'

/**
 * Step III: the Fame objectives are checked, and the tiles follow. Nobody chooses anything, so the
 * header only names the moment — the tile changing hands says the rest (see {@link CheckFameRule}).
 */
export const CheckFameHeader = () => <HeaderText code="check-fame" />
