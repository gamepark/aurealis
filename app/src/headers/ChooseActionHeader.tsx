import { HeaderText } from '@gamepark/react-game'

/**
 * Steps I and II of a turn: the three actions are on the table at once, so the header only names
 * them (see {@link ChooseActionRule}).
 */
export const ChooseActionHeader = () => <HeaderText code="choose-action" />
