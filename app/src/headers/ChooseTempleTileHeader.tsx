import { HeaderText } from '@gamepark/react-game'

/** One of the Temple tiles on display, whose effect applies at once (see {@link ChooseTempleTileRule}). */
export const ChooseTempleTileHeader = () => <HeaderText code="choose-temple-tile" />
